name: 🏈 NFL Live (Daily Planner)

on:
  # Run frequently on typical NFL days to refresh live lines, update finals, etc.
  schedule:
    # Thu–Mon: every 10 minutes from ~11:00 AM ET through ~2:00 AM ET
    - cron: "*/10 16-6 * * 4-1"
  workflow_dispatch:

jobs:
  watchers:
    name: live
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install dependencies
        run: |
          npm install googleapis axios

      - name: Update live board (today) + finals sweep
        env:
          GOOGLE_SHEET_ID: ${{ secrets.GOOGLE_SHEET_ID }}
          GOOGLE_SERVICE_ACCOUNT: ${{ secrets.GOOGLE_SERVICE_ACCOUNT }}
          LEAGUE: nfl
          TAB_NAME: NFL
          PREFILL_MODE: today
        run: node orchestrator.mjs
